<h1>Daily meal planner</h1>


<%= form_for daily_food_planner_plan_path, method: 'POST' do %>
    <div class="row">

      <% ingredients = Ingredient.all.order('name ASC') %>

      <section>
        <select name="ingredient0">
          <option value="0">Please select an ingredient</option>

          <% for each in ingredients %>
              <option value="<%= each.name %>"><%= each.name %> (<%= each.serving_type%>) </option>
          <% end %>
        </select>

        <%= text_field_tag :quantity0, nil, placeholder:'Enter quantity'%>
      </section>

      <section>
        <select name="ingredient1">
          <option value="0">Please select an ingredient</option>

          <% for each in ingredients %>
              <option value="<%= each.name %>"><%= each.name %>  (<%= each.serving_type%>) </option>
          <% end %>

        </select>

        <%= text_field_tag :quantity1, nil, placeholder:'Enter quantity'%>
      </section>

      <section>
        <select name="ingredient2">
          <option value="0">Please select an ingredient</option>

          <% for each in ingredients %>
              <option value="<%= each.name %>"><%= each.name %>  (<%= each.serving_type%>) </option>
          <% end %>

        </select>

        <%= text_field_tag :quantity2, nil, placeholder:'Enter quantity'%>
      </section>

      <section>
        <select name="ingredient3">
          <option value="0">Please select an ingredient</option>

          <% for each in ingredients %>
              <option value="<%= each.name %>"><%= each.name %>  (<%= each.serving_type%>) </option>
          <% end %>

        </select>

        <%= text_field_tag :quantity3, nil, placeholder:'Enter quantity'%>
      </section>

      <section>
        <select name="ingredient4">
          <option value="0">Please select an ingredient</option>

          <% for each in ingredients %>
              <option value="<%= each.name %>"><%= each.name %>  (<%= each.serving_type%>) </option>
          <% end %>

        </select>

        <%= text_field_tag :quantity4, nil, placeholder:'Enter quantity'%>
      </section>

      <section>
        <select name="ingredient5">
          <option value="0">Please select an ingredient</option>

          <% for each in ingredients %>
              <option value="<%= each.name %>"><%= each.name %>  (<%= each.serving_type%>) </option>
          <% end %>

        </select>

        <%= text_field_tag :quantity5, nil, placeholder:'Enter quantity'%>
      </section>


    </div>

    <br>

    <%= submit_tag 'Submit', id: 'submit' %>
<% end %>


<% unless @results.empty? %>
    <div class="row">

      <section>
        <h2>Ingredients:</h2>
        <% for meal in @meals %>
            <p><%= meal[0] %> - <%= meal[1] %> <%= meal[2] %></p>
        <% end %>
      </section>

      <section>
        <h2>Nutrient totals:</h2>

        <p> Proteins - <%= @results[:prots] %> g</p>

        <p> Carbs - <%= @results[:carbs] %> g</p>

        <p> Fats - <%= @results[:fats] %> g</p>
      </section>

    </div>
<% end %>


